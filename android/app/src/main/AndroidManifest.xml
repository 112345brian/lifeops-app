<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed for NextTasksRefreshWorker's periodic GET/POST calls and for
         registering the FCM token with the server (the briefing push itself,
         once delivered, needs no permission: Firebase handles that). -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- LocationReporter's one-shot GPS fix (see that file for the
         reporting cadence), POSTed to /api/location so weather.py can use
         the phone's actual position instead of a fixed WEATHER_LAT/LON.
         FINE, not just COARSE, so the resolved forecast grid cell is
         accurate; BACKGROUND is required on API 29+ because the fetch runs
         from NextTasksRefreshWorker (a WorkManager job), which the OS
         treats as background execution even though the app itself is
         never foregrounded for it; without this permission a background
         getCurrentLocation() call silently yields nothing on Android 10+. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Per-widget-instance display customization, launched by the
             App Widget host (a different process/UID, hence exported=true,
             same reasoning as SettingsActivity above) whenever a widget is
             added or the user chooses to reconfigure it. -->
        <activity
            android:name=".WidgetConfigActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:excludeFromRecents="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".BriefingWidgetReceiver"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/briefing_widget_info" />
        </receiver>

        <!-- Single-stat presets: separate pickable entries in the OS
             widget tray, see BriefingWidgetReceiver.kt's docstring. -->
        <receiver
            android:name=".GymWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_name_gym">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/gym_widget_info" />
        </receiver>

        <receiver
            android:name=".MoneyWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_name_money">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/money_widget_info" />
        </receiver>

        <receiver
            android:name=".CourseworkWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_name_coursework">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/coursework_widget_info" />
        </receiver>

        <receiver
            android:name=".WeatherWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_name_weather">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/weather_widget_info" />
        </receiver>

        <receiver
            android:name=".SleepWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_name_sleep">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/sleep_widget_info" />
        </receiver>

        <receiver
            android:name=".SocialWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_name_social">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/social_widget_info" />
        </receiver>

        <!-- Receives the daily briefing via Firebase Cloud Messaging:
             reliable even when the app is stopped, unlike a manifest
             broadcast receiver for a third-party implicit broadcast. -->
        <service
            android:name=".BriefingFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
