<?xml version="1.0" encoding="utf-8"?>
<!-- Single-stat preset (coursework only). The real solo rendering is a
     label/value/status SoloStatCard with the long "COURSEWORK" label, so
     this defaults to a compact 2x1 strip rather than forcing a cramped 1x1.
     Users can still resize it larger. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="56dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="56dp"
    android:maxResizeWidth="250dp"
    android:maxResizeHeight="276dp"
    android:targetCellWidth="2"
    android:targetCellHeight="1"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:updatePeriodMillis="1800000"
    android:description="@string/widget_description_coursework"
    android:initialLayout="@layout/widget_loading"
    android:previewLayout="@layout/widget_preview_coursework"
    android:configure="com.lifeops.briefing.WidgetConfigActivity"
    android:widgetFeatures="reconfigurable" />
