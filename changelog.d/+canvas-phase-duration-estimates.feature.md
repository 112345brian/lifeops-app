Content-aware phase names now come with real per-phase time estimates too, based on the actual question count/point values/complexity in the assignment's description and linked files — not the same fixed generic durations (e.g. 80/105/75 minutes) applied to every assignment of a given type regardless of how much work it actually involves. Falls back to the generic fixed durations whenever a content-aware estimate isn't available.
