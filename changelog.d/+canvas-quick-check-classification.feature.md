Fixed a 0-point, external-tool-submitted assignment (a practice/skills check, typically an embedded publisher quiz) getting the generic "assignment" atype's 260-minute no-due-date fallback duration — these had no due date to anchor phase-splitting/content-aware labeling on, so they always fell through to the biggest, vaguest estimate available. Now classified as its own lightweight "quick_check" type (25 minutes, single task) when both signals — zero points and an external-tool submission — are present together.
