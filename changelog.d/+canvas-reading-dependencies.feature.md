Readings now depend on the previous module's readings (each module's first new reading is `blockedBy` the last reading of the prior module), the same dependency mechanism already used for multi-phase assignments. Previously readings had no ordering at all, so nothing stopped a later module's reading from being scheduled before an earlier one's — especially on a first sync, where Canvas often reports every already-published module as unlocked "today" with no real gating between them.
