`ChoreEngine.kt`/`SocialEngine.kt` were replaced by `ChoreSchedule.kt`/`SocialSchedule.kt` (same `plan()` behavior, all 12/7 original test scenarios reproduced unchanged). Unlike gym's earlier migration to `Routine`/`TimeSlot`/`LifeScript`, chore and social have no per-day slot-picking to decompose — both already routed their due-check through `Routine.kt`'s shared primitives, so the rename documents that finding rather than forcing an unneeded `TimeSlot` layer.
