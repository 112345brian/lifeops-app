The on-device compute tick now creates a chore's next occurrence itself: when a `[cycle:Nd]`-tagged FlowSavvy task is seen completed, it computes the next due date and creates the follow-up task on the phone, with a persisted dedup record so the same completion is never cycled twice. This closes the last write-capable gap called out in the on-device compute tick's own rollout notes.
