Fixed a bug where the on-device compute tick, when FlowSavvy wasn't yet configured, bailed out entirely before ever regenerating the widget's attention/briefing state -- confirmed live-device as Attention-tab reason cards showing the bare word "coursework" (a null-title fallback) and a blank Today-tab headline, both traced to stale/foreign data left sitting in `BRIEFING_JSON` forever with nothing to overwrite it. The tick now always runs and writes a real, correctly-titled attention state from every local-only fact source (gym, social/meal nudges, discretionary dollars, system health), skipping only the FlowSavvy-dependent pieces (coursework facts, chore/gym-plan write-back) that genuinely need FlowSavvy data.
