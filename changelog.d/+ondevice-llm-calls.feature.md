Moved the weekly accountability digest's LLM call on-device: `LifeOpsComputeWorker`'s 15-min tick now generates and sends the Sunday digest directly via a new `AnthropicClient`, self-gated once/week, with no biometric prompt (background automation). Added encrypted on-device storage and a Settings field for the Anthropic API key. Added a reusable `BiometricGate` primitive for future user-initiated actions (not wired to anything yet). The daily-briefing narrative LLM call and full on-device YNAB novel-payee write pipeline remain out of scope for this pass — see `AnthropicClient.kt`'s kdoc for why.
