Added an on-device `SystemHealth` signal for the attention model: per-data-source (FlowSavvy, YNAB) "last successful sync" freshness, computed from real on-device sync attempts rather than a port of the server's automation-process-health concept, which has no phone-side equivalent. A stale or failed sync now correctly raises attention severity instead of the compute tick always passing `null`.
