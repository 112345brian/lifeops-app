Runner state logs now live under the `private` submodule instead of the
main repo's `logs/` directory, and each run auto-commits and pushes that
submodule's state so it stays backed up instead of living only on local
disk.
