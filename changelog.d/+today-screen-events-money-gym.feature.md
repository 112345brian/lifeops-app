The Today screen now surfaces three sections it previously left unrendered even though `BriefingState` already carried the data: a "This week" section listing `notableEvents` (haircuts, one-off appointments, "movie on Wednesday" -- the rolling next-7-days heads-up, as opposed to the existing "Today's events" section which only ever showed same-day events), a "Money" card (the widget's own "safe to spend" framing -- label/status text like `TODAY`/`LEFT`/`OVER`, not a raw balance), and a "Gym" card (`gymLast7d`/`gymTarget` as a `3/4 this week` stat). All three reuse `BriefingWidget.kt`'s own presentation logic (`moneyStatPresentation`, `gymFallbackStatPresentation`, `notableEventDay`/`notableEventTime`) so the in-app screen can't drift from what the Glance home-screen widget already shows for the same fields, styled to match this screen's existing `Card`/`SectionLabel`/`severityDotColor` conventions.
